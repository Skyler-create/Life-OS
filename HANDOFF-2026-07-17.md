# Handoff — routine merges pending (session of 2026-07-17)

Everything below is ALREADY LIVE in Supabase (`xebiaoycxwstrzgkjult`) and in
the deployed PWA. The only thing left is merging new instructions into the
live routines at claude.ai/code/routines, whose text does not exist on disk.

---

## New backend surface the routines can now call

| call | returns |
|---|---|
| `select lifeos_plan_context();` | ONE call: date + weekday, `is_weekend`, `now_local`, freezes, yesterday's log, today's log, events, timetree, `free_windows`, **all open tasks with `feasible_today` + `infeasible_reasons`**, weekly goal gaps, `patterns` (focused min by hour + weekday over 28d, typical first clock-in, 14d adherence), sleep stats, streak |
| `select * from lifeos_task_feasible(constraints, '<date>');` | `(ok boolean, reasons jsonb)` for one task's constraints on a date |
| `select lifeos_news_prefs();` | `topic_weights`, `recent_votes` (last 14d), and a `directive` string about discovery items |
| `select lifeos_stocks_context();` | existing keys + **`unregistered_trades`** (brokerage buys with no holdings row; each has `key`, `posted`, `payee`, `amount`, `institution`, `account`) |
| `select lifeos_timeline(14);` | unified focus + scroll session timeline |

### New/changed columns
- `tasks.constraints jsonb` — keys: `days` (ISO array, 1=Mon..7=Sun **allowed**),
  `end` ("HH:MM" daily window), `not_before` (date), `place`, `needs` (free text).
- `focus_sessions.credit_rate` (1 = plan/START HERE, 0.333 = off-plan/general)
  and `focus_sessions.is_starter` — both stamped automatically by a trigger.
- `stock_trade_acks(key)` — dismissed/handled trades.

### New credit rules (context for tone, not something routines write)
- The day's 10 free credits are **locked** until 10 clocked minutes on the
  START HERE task. No brief-click loophole.
- Off-plan/general clock-ins earn **⅓** rate; plan tasks earn full.
- Reflecting (or logging a rest day) pays **+5 free credits the next day**.
- Push at 22:00 nudges reflection, gated on it not being written yet.

---

## MERGE 1 — Morning brief routine (claude.ai/code/routines)

Keep ALL existing personalization (schedule/energy rules, weekly targets,
learning-topic rotation, tone, project ref). Only the Steps change:

1. **READ step** — replace the pile of separate reads with
   `select lifeos_plan_context();`. Keep "last 5 briefs for topic rotation"
   and the Gmail read.

2. **NEW step — CONSTRAINTS BACKFILL** (right after READ): for open tasks
   where `constraints` is null but title/notes imply real-world requirements
   (a person, a place, business hours, weekdays, a delivery not yet arrived),
   write them back:
   ```sql
   update tasks set constraints='{"days":[1,2,3,4,5],"end":"17:00","place":"office","needs":"Lydia in office"}' where id='<id>';
   ```
   Only add what's clearly implied — no constraint is better than a wrong one.

3. **NEW step — AGENT-DOABLE TASKS** (right after INBOX TRIAGE): any open task
   the routine can complete itself with data at hand (add a flight/appointment
   to `events`, a lookup, a calculation, drafting text into notes) — EXECUTE it.
   Then prepend to that task's notes:
   `🤖 DONE BY AI <date>: <what you did + where to verify>. Verify, then mark done.`
   and **leave the task open** — Skyler crosses it off after reading. Never
   silently do work he can't see; never mark it done yourself. Mention
   AI-completed items in one REALITY CHECK line.

4. **START HERE — feasibility is a HARD RULE**: only tasks with
   `feasible_today=true` are eligible for START HERE or the core plan.
   Infeasible ones get a one-line `PARKED (why)` in REALITY CHECK, never the
   plan. Sanity-check the rest against reality too (weekend => nothing needing
   the office; all-day travel => PARALLEL/LIGHT day). Then the existing rules
   (hard beats self-imposed, overdue first, energy match, first not-started
   chunk). START HERE must open with a **≤15-min concrete first move**
   ("open notes, do 3 problems") — and the brief should say that the first
   10 clocked minutes on it unlock the day's free screen credits.

5. **PLAN — implementation intentions**: every plan item gets a `"when"` — a
   concrete trigger anchored in today's real `free_windows` and his actual
   peak hours from `patterns` (e.g. "13:00, right after lunch", "when you get
   home from Lenovo"). Phrase plan lines in the brief as if-then
   ("At 1 PM at your desk -> Thermal 1/4"), not as a to-do list.
   *(Why: Gollwitzer & Sheeran meta-analysis, 94 studies, d=0.65 — the single
   strongest lever available here.)*

6. **WRITE step** — `plan` JSON items now carry `"when"`:
   `[{"title","est_minutes","energy","priority","when"}]`.
   REALITY CHECK also lists PARKED items with the one-line why.

7. **SELF-CHECK** — also redo the upsert if `start_here_task` matches a task
   whose `feasible_today` was false.

---

## MERGE 2 — Radio/news routine (claude.ai/code/routines)

Add one step before news selection:

> Run `select lifeos_news_prefs();` and follow its `topic_weights` and
> `directive`. Weights tune HOW MUCH of a liked topic to serve — not the whole
> mix. Every day include **2–3 DISCOVERY items deliberately outside** the
> weighted topics: world affairs, geopolitics, economics, science, health, or a
> major story most informed people would know. Mark them `topic='discovery'`.
> Goal: keep the topics he already likes AND make him more worldly. If a
> discovery item gets a 👍, it graduates into the rotation.

(The 👍/👎 buttons already write `news_items.feedback` and adjust
`lifeos_settings.news_topic_weights` — that half works today.)

---

## MERGE 3 — Stocks analyst routine (NEW — create it)

Drop in `template/routine_stocks_daily.md` as-is. Trigger: weekdays ~6:30 PM ET
(after close). Connectors: Supabase + web search. Replace `__PROJECT_REF__` with
`xebiaoycxwstrzgkjult`.

Why it's needed: `stock_reports` has **0 rows** — the tables, indicators and the
Analyst card in MONEY → STOCKS were all built, but nothing ever wrote a report.

Key detail it handles: SimpleFIN sends E*Trade trades as
`"MICRON TECH INC UNSOLICITED TRADE" / memo "Bought" / -$1,760` — **no ticker,
no share count, no price**. So the routine infers ticker from company name, gets
the trade-date close, estimates `shares = |amount| / close`, inserts a holding
marked "🤖 estimated — confirm", syncs quotes, acks the trade, and pushes Skyler
to confirm. Morgan Stanley account (0378) = Rod's advisor-managed — mention
activity, never auto-register, never nag.

---

## Also worth knowing

- **Evening reflection routine** (if it exists as a routine): reflection now
  pays +5 credits next day; the 22:00 push already fires only when the
  reflection is unwritten. If the evening routine writes `daily_log`, nothing
  changes — saves are upserts and stay re-editable all day.
- **The chat edge function** (`chat`, v11) got a REALITY RULE: it may never
  claim a write it didn't execute (it must check `rows_affected` and re-select
  to confirm), a real swap-START-HERE verb, and a ban on marking tasks done
  unless Skyler says he did them. This came from a real incident: chat claimed
  it swapped START HERE off "Go find Lydia", didn't, and the running clock
  auto-completed the task for +5 credits. Reverted.
